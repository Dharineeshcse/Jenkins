pipeline {
    agent{
        label 'self-hosted-linux'
    }

    stages {
        stage('Build') {
            steps {
                echo "Development Build"
                sleep 10
            }
        }
    }
}