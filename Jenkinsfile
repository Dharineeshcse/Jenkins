pipeline {
    agent{
        label 'self-hosted-linux'
    }

    stages {
        stage('Build') {
            steps {
                echo "Production Build"
                sleep 10
            }
        }
    }
}